extends ../app.pug

block content
    form(action="/clients", method="post")
        label(for="name") Name:
        br
        input#name(type="text" name="name" value="" placeholder="Enter client name" required autofocus)
        br  
        label(for="email") Email:
        br
        input#email(type="email" name="email" value="" placeholder="Enter client email" required)
        br
        button(type="submit" value="submit") Create Client 